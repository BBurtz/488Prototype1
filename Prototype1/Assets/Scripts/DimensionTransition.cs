using UnityEngine;

public class DimensionTransition : MonoBehaviour
{
    /*
     * variables: player offset from wall --
     * needs to check if player is able to shift
     * two players, one on one off
     * mirrors player's movement
     * 
     * calculate distance from wall point to reflect
     */

    //public float MirrorOffset;
    private Transform DimensionWall;

    private void Start()
    {
        
    }
    private void FixedUpdate()
    {
        
    }
}
